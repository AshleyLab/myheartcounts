library(TSclust)
#x <- as.matrix(read.table('/home/annashch/timeseries/motiontracker.tsv', header=TRUE, sep = "\t",row.names=1,as.is=TRUE))
x <- read.table('/home/anna/masamune/annashch/timeseries/motiontracker.tsv', header=FALSE, sep = "\t")
